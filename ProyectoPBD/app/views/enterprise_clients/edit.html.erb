<% if user_signed_in? and current_user.tipo == 'administrador' %>

	<h1>Editing enterprise_client</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @enterprise_client %> |
	<%= link_to 'Back', enterprise_clients_path %>
<% end %>