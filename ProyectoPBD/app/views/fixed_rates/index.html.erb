<h1>Listing fixed_rates</h1>

<table>
  <thead>
    <tr>
      <th>Ori tar</th>
      <th>Dest tar</th>
      <th>Costo tar</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @fixed_rates.each do |fixed_rate| %>
      <tr>
        <td><%= fixed_rate.ori_tar %></td>
        <td><%= fixed_rate.dest_tar %></td>
        <td><%= fixed_rate.costo_tar %></td>
        <td><%= link_to 'Ver', fixed_rate %></td>
        <td><%= link_to 'Editar', edit_fixed_rate_path(fixed_rate) %></td>
        <td><%= link_to 'Eliminar', fixed_rate, method: :delete, data: { confirm: '¿Confirma eliminación?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo', new_fixed_rate_path %>
